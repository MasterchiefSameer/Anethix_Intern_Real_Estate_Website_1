# Daily Work Report - July 13, 2026

## 🎯 Focus: Backend Setup & User Sign-Up API

Today was focused on launching the Node.js / Express backend server, establishing connection with MongoDB via Mongoose, and implementing the user sign-up route with secure password hashing.

---

## 🛠️ Work Done

### 1. Express Server Integration
* Configured `backend/index.js` to initialize the Express app, parse incoming JSON, and listen on port `3000`.
* Standardized dotenv config to fetch secrets like the MongoDB connection URI from a `.env` file.

### 2. User Schema Definition
* Defined the Mongoose User Schema in `backend/models/user.model.js`.
* Ensured fields like `username`, `email` (unique), and `password` are present and validated.

### 3. Test Routing
* Set up a test API route (`/api/user/test`) to verify API structure and JSON responses.

### 4. User Sign-Up Functionality
* Created the `/api/auth/signup` endpoint.
* Implemented password security: integrated `bcryptjs` to hash user passwords before storing them in MongoDB.
* Created corresponding route handlers and controller logic inside `backend/controllers/auth.controller.js` and `backend/routes/auth.route.js`.

---

## 📂 Files Created / Modified

| Action | File Path |
| :--- | :--- |
| **Added** | `backend/controllers/user.controller.js` |
| **Added** | `backend/routes/user.route.js` |
| **Added** | `backend/models/user.model.js` |
| **Added** | `backend/routes/auth.route.js` |
| **Added** | `backend/controllers/auth.controller.js` |
| **Modified** | `backend/index.js` |
| **Modified** | `package.json` / `package-lock.json` |
